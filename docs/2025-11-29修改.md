### 首页

- 首页默认是全部收起的。如果有选中的学期，则展开。

### 做题页

- 不会的字，出完正确的字，时间暂停，需手动点击下一题。为了有时间认识巩固生字
- 页面紧凑一些，去掉田字格上面「识别中」一栏，田字格往上顶替这一栏空间
- 答题数变成可视化的形式，除了数字之外，用格子显示还剩多少个字，完成的格子，如果对的就是绿色方块，错的就是红色方块，8*8px的格子。没完成的用灰色的格子。数字在最后显示
- 在做完所有的题目（大于50题时）时，做到最后一道题时（可能是最后一道题），弹出弹窗告知出错、本轮结果未保存。在错题本中的「按轮视图」中无法看到本轮练习的任务记录，但在「汇总视图」中可以看到。请设置一个机制，确保就算中途出问题、退出，练习的记录也会被记录在「按轮视图」中
- 请确保，用户一笔都没有写，直接点击下一题的时候不会触发文字识别，不走API验证，节省成本。我现在点击下一题，仍然看到有提示：识别中。

### 结果页

- 将正确率改成分数。比如正确率80%就显示80分即可，这样小孩更容易懂，保留正确X，错误Y的统计。分数放在统计数据的第一个，其他的几个统计数据字小一点
- 结果中分成错误的和正确的两个部分，先显示错误的，下面的显示正确的

### 任务清单

- 当题目大于50题，触发拆题时，拆成了两个任务，但在实际做一个任务时，中间并没有被拆开，而是连续做完了所有题目。
- 点击「查看已完成」，并没有切换到已完成的任务
- 对于未排期的任务，在ipad上点开日期菜单，还没有动手去点的时候，会自动选择一个日期，然后提交确认，这个不合理。而且一旦选择日期后，点击「还原」，无法清空日期。无排期的任务不应该出现在「未来任务」中。「今天」和「未来任务」这两个标题和下面的卡片的间距太大了。
- 去掉「复习计划」的单独页面，因为复习计划是根据错题自动生成复习任务，拆分到每一天中去，放在「任务清单」中的


---

已阅读需求文档。需要澄清以下问题：

## 需要澄清的问题

### 1. 做题页 - 答题数可视化
- “数字在最后显示”是指：格子后面显示“X/总数”，还是只在格子区域末尾显示数字？
- 格子排列：一行多少个？超出时是否换行？

### 2. 做题页 - 大任务（>50题）保存机制
- “中途出问题、退出”具体指哪些场景？
  - 浏览器关闭/刷新？
  - 网络中断导致识别失败？
  - 用户主动点击“结束练习”？
- 期望的保存时机：
  - 每答完一题自动保存？
  - 每答完 N 题保存一次？
  - 页面可见性变化时保存？

### 3. 做题页 - 空画布点击“下一题”
- 当前“识别中”提示出现在哪里？
  - 是点击“下一题”按钮时立即显示？
  - 还是点击后触发了识别 API 调用？
- 期望行为：
  - 直接跳过，不显示任何提示？
  - 还是显示“跳过本题”之类的提示（但不调用 API）？

### 4. 任务清单 - 拆题后连续做完
- “中间没有被拆开，而是连续做完了所有题目”是指：
  - 拆成任务A（1-50）和任务B（51-100），但做任务A时把1-100都做完了？
  - 还是任务A做完后没有自动停止，继续做了任务B？
- 期望行为：
  - 任务A做完后自动停止，需要手动开始任务B？
  - 还是任务A做完后提示“任务完成，是否继续下一个任务”？

### 5. 任务清单 - 复习计划整合
- 去掉“复习计划”页面后：
  - 导航栏的“复习计划”链接是否也删除？
  - 复习任务在任务清单中如何区分显示（例如用不同图标/标签）？
  - 是否需要保留“刷新复习计划”的功能入口？放在哪里？

### 6. 任务清单 - iPad 日期选择器
- “还没动手就自动选择日期”是指：
  - 打开日期选择器时自动选中今天？
  - 还是鼠标/手指悬停就触发选择？
- “点击「还原」无法清空日期”：
  - 是否有“还原”按钮？在哪里？
  - 期望行为是点击后日期输入框变为空，任务回到“待排期”？

请确认以上问题，我会据此实现。